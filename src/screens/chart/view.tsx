import React from 'react';
import {BaseScreen, Text} from '@components/index';
import {Strings} from '@constants/index';

const ChartScreen = () => {
  return (
    <BaseScreen title={Strings.yearConsolidated}>
      <Text>Screen</Text>
    </BaseScreen>
  );
};

export default ChartScreen;
